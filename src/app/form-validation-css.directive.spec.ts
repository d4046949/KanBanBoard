import { FormValidationCssDirective } from './form-validation-css.directive';

describe('FormValidationCssDirective', () => {
  it('should create an instance', () => {
    const directive = new FormValidationCssDirective();
    expect(directive).toBeTruthy();
  });
});
